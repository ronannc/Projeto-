<div class="form-group">
    <label for="nascimento">Nascimento</label>
    <input type="date" class="form-control" name="nascimento" id="input_nascimento" placeholder="Nascimento" value ="{{ old('nascimento') ?? $data['nascimento'] ?? null }}">
</div>
