<div class="form-group">
    <label for="abdomen">Sexo</label>
    <input type="text" class="form-control" name="sex" id="sex" placeholder="Sexo"
           value="{{ old('sex') ?? $data['sex'] ?? null }}">
</div>
